//chalk pozwala wyświetlić komunikat w podanym kolorze
//poniżej kod z Babel - pisanie w modernJS, który kompiluje sie do zwykłego JS
import chalk from 'chalk';

console.log(chalk.green('Starting app in dev mode...'));
